const { defineConfig, devices } = require('@playwright/test');

// End-to-end tests drive the real apps in a headless browser against the Vite
// dev server. The apps load a few libraries from CDNs (pdf.js, JSZip, pdf-lib),
// so these tests need network access, same as the apps themselves.
module.exports = defineConfig({
  testDir: 'e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: process.env.CI ? 'line' : 'list',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
  },
});
